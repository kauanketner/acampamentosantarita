// TODO: integração com Better Auth via cookie de sessão.
// Funções esperadas:
// - useSession() — retorna { user, isLoading }
// - login(email, password)
// - logout()
// - guard de rota: redirecionar pra /login quando não autenticado.

export const auth = {};
