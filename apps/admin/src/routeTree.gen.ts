// Este arquivo é gerado automaticamente pelo plugin TanStackRouterVite ao rodar
// `bun run dev` ou `bun run build`. O conteúdo abaixo é apenas um placeholder
// para o TypeScript não reclamar antes do primeiro build. NÃO EDITAR À MÃO.

import { createRootRoute } from '@tanstack/react-router';

export const routeTree = createRootRoute();
