// Gerado automaticamente pelo plugin TanStackRouterVite. NÃO EDITAR À MÃO.
// O conteúdo abaixo é apenas placeholder até o primeiro `bun run dev`/`build`.

import { createRootRoute } from '@tanstack/react-router';

export const routeTree = createRootRoute();
