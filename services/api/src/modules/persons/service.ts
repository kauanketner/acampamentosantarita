// TODO: CRUD do cadastro central (persons + emergency_contacts).
// fullProfile deve agregar: persons + healthProfiles + faithProfiles +
// receivedSacraments + emergencyContacts + campParticipations.

export const personsService = {};
