// TODO:
// - createAnnouncement com targetAudience: filtra pessoas e cria notifications + envia push.
// - resolverAudiencia('todos' | 'participantes_evento' | 'equipistas' | 'tribo_x' | 'equipe_x', targetId)
// - lib/push.ts envia em paralelo com retry; remover subscriptions inválidas (410/404).

export const communicationService = {};
