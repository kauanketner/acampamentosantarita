// TODO: cliente Cloudflare R2 (S3 compatível).
// - putObject(key, body, contentType) → URL pública (R2_PUBLIC_URL/key)
// - deleteObject(key)
// - getSignedUploadUrl(key, contentType) — para upload direto do client.

export const r2 = {};
