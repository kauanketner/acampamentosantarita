// TODO: web-push wrapper.
// - sendToSubscription(subscription, payload)
// - sendToPersons(personIds[], payload) — busca subscriptions e envia em paralelo,
//   removendo as que retornam 410/404 (sub. inválida).

export const push = {};
