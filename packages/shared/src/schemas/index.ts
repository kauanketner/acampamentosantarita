// TODO: schemas Zod compartilhados entre back e front.
// Cada módulo do API terá seus schemas em services/api/src/modules/*/schemas.ts;
// quando o schema for genuinamente compartilhado (ex: cadastro inicial, payloads de webhook,
// DTOs do public API consumidos pelo Next), promova para cá e exporte por nome.

export {};
